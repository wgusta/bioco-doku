# bioco-doku archived

Internal documentation now lives in **ProcessWire** under `/internal-docs/` in the bioco-web-project CMS. The MkDocs site and `docs.bioco.ch` deployment are retired.

- Do not re-enable automatic MkDocs deploy workflows without an explicit decision.
- Remove the accidental nested `bioco-web-project/` copy from this repository when convenient (`git rm -r --cached bioco-web-project` if it was committed).

Historical Markdown remains in `docs/` for reference until you delete it after a successful CMS migration.
